.explore/
# wrangler (just dev, just deploy): local state, local secrets, host-installed packages
.wrangler/
.dev.vars
node_modules/
# just local: fonts downloaded from Google Fonts to run the site offline (tools/local.py)
local/
