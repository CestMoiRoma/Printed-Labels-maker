{
  "name": "printed-labels-maker",
  "version": "1.0.0",
  "private": true,
  "description": "Box label generator, served as static assets by a Cloudflare Worker (no build: site/ is deployed as is).",
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "wrangler": "4.134.0"
  }
}
